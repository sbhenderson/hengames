{
  "name": "@hengames/game-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build -w @hengames/shared && vitest run"
  },
  "dependencies": {
    "@hengames/shared": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^3.1.4"
  }
}
